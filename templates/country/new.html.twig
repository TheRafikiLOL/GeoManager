{# templates/country/new.html.twig #}
<h1>Create Country</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Create</button>
{{ form_end(form) }}

<a href="{{ path('app_country') }}" class="btn btn-secondary">Back to List</a>
